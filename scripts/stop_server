#!/bin/bash

# Enable debugging
set -e  # Exit immediately if a command fails
set -x  # Print each command before executing it

# Stop and remove the container
sudo docker stop final-python-project || { echo "Failed to stop container"; exit 1; }
sudo docker rm final-python-project || { echo "Failed to remove container"; exit 1; }
